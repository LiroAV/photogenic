# Photogenic

A cozy local-first photo album web app for daily picture challenges.

## Run locally

```bash
python3 -m http.server 4173
```

Then open:

```text
http://localhost:4173/
```

Photos and albums are stored in the browser with `localStorage`.
